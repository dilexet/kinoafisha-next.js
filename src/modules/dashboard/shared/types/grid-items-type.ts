export interface GridItemsType {
  property: string;
  value: string | number | undefined | null;
}

export interface GridTextFieldsType {
  id: string;
  name: string;
  label: string;
  type: string;
}
